const Carrier = require('./Carrier');
const Broker = require('./Broker');
const Invoice = require('./Invoice');
const User = require('./User');
const Payment = require('./Payment');

module.exports = { Carrier, Broker, Invoice, User, Payment };